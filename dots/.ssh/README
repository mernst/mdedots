I don't want to have symbolic links like these:
  id_rsa -> csail
  id_rsa.pub -> csail.pub
because then various programs like CVS ask for the passphrase even if I
am using Kerberos for that particular connection.
